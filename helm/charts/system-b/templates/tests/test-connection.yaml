apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "system-b.fullname" . }}-test-connection"
  labels:
    {{- include "system-b.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "system-b.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
