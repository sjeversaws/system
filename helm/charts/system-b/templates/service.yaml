apiVersion: v1
kind: Service
metadata:
  name: {{ include "system-b.fullname" . }}
  labels:
    {{- include "system-b.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "system-b.selectorLabels" . | nindent 4 }}
